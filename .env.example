# Exemplo de arquivo .env para desenvolvimento
# NÃO COMMITAR o arquivo .env com segredos reais em repositórios públicos.

# App
NODE_ENV=development
PORT=3000
DEV_PORT_FALLBACK=1   # 1 habilita tentativa de porta alternativa quando 3000 estiver ocupada (opcional)
BASE_URL=http://localhost:3000

# Banco de dados
# DATABASE_CLIENT pode ser 'sqlite3' ou outro cliente suportado (pg, mysql, etc.)
DATABASE_CLIENT=sqlite3
# Se using sqlite3, a conexão é por filename
DATABASE_FILENAME=./dev.sqlite3
# Para outros clients (e.g. postgres) use DATABASE_URL no formato apropriado
DATABASE_URL=

# Autenticação / segurança
# Use um segredo forte em produção (gerado aleatoriamente com pelo menos 32 bytes)
JWT_SECRET=replace_with_a_strong_random_secret
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_DAYS=7

# Senha/bcrypt
SALT_ROUNDS=10

# Configurações úteis para testes / scripts
# Se você executar testes com Jest, JEST_WORKER_ID é injetado automaticamente pelo Jest.

# Nota: algumas variáveis têm valores padrão hardcoded no código quando não encontradas.
# Ex.: SALT_ROUNDS default = 10, ACCESS_TOKEN_EXPIRY default = '15m', REFRESH_TOKEN_DAYS default = 7.
